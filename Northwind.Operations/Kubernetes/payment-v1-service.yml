apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    api: payment
    version: "1"
spec:
  type: ClusterIP
  selector:
    api: payment
    version: "1"
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80