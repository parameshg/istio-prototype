apiVersion: v1
kind: Service
metadata:
  name: product
  labels:
    api: product
    version: "1"
spec:
  type: ClusterIP
  selector:
    api: product
    version: "1"
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80