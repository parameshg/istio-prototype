apiVersion: v1
kind: Service
metadata:
  name: order
  labels:
    api: order
    version: "1"
spec:
  type: ClusterIP
  selector:
    api: order
    version: "1"
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80