apiVersion: v1
kind: Service
metadata:
  name: address
  labels:
    api: address
    version: "1"
spec:
  type: ClusterIP
  selector:
    api: address
    version: "1"
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80